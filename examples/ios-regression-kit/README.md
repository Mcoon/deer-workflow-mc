# iOS Regression Kit

Shared types and deterministic helpers for the iOS UI discovery, case
authoring, and functional regression Workflows. This directory is a library,
not a standalone Workflow.

It keeps natural-language intent separate from executable page IDs, control
IDs, selectors, assertions, and device-profile coordinate bindings.
