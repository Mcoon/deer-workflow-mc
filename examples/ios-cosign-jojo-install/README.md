# Compatibility entry point

This legacy path now delegates to
[`ios-cosign-bitsky-install`](../ios-cosign-bitsky-install/README.md). JoJo is
not invoked. New callers should use the BitSky path directly.
